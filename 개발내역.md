# 투자 허브 개발 내역

> 작업 파일: `index.html` (단일 파일 SPA)
> 배포: GitHub Pages — https://jinhoo111.github.io/-_-/
> 백엔드: Supabase Edge Function — https://yijkwuiqnviapztqskak.supabase.co/functions/v1/proxy-api

---

## 세션 1 — 고도화 전략 수립

### 작업 내용
- **NSM(North Star Metric)** 정의: 주간 활성 포트폴리오 업데이트 수
- **Input Metrics** 5개 정의 (종목 추가율, 뉴스 조회율, 메모 작성율, 가격 조회 성공률, API 키 등록율)
- 기능 우선순위 분류: Killer / Core / Nice-to-have
- **산출물**: `idea_richbuild_v2.md`

---

## 세션 2 — 기반 강화 Phase 1: API 키 서버 이관 (Supabase)

### 배경
- 기존 구조: API 키를 브라우저 `localStorage`에 평문 저장 → 노출 위험
- 목표: 서버 측(Supabase)에 AES-256-GCM 암호화 후 저장, 클라이언트는 키를 직접 보유하지 않음

### 인프라 구축
1. **Supabase 프로젝트 생성** — `investment_news service`
2. **DB 테이블 생성** (`api_keys`)
   ```sql
   create table api_keys (
     service text primary key,
     encrypted_key text not null,
     iv text not null,
     updated_at timestamptz default now()
   );
   ```
3. **Edge Function 배포** — `proxy-api` (`supabase/functions/proxy-api/index.ts`)
   - AES-256-GCM 암호화/복호화 구현 (Deno Web Crypto API)
   - 액션 4종: `store-key` / `key-exists` / `delete-key` / `call`
   - 서비스 2종: `finnhub` (REST 프록시), `googleai` (Gemini API 프록시)
4. **환경변수** `ENCRYPTION_SECRET` Supabase Dashboard에서 설정
5. **인증**: Supabase Publishable Key를 `apikey` + `Authorization: Bearer` 헤더로 전달

### index.html 변경 사항
```js
// 추가된 전역 상수
const SUPA_URL = 'https://yijkwuiqnviapztqskak.supabase.co/functions/v1/proxy-api';
const SUPA_KEY = 'sb_publishable_xa9hIIRRb6_YWc8MGWNhlA_3Ch6NAiF';
const _sh = {'Content-Type':'application/json','apikey':SUPA_KEY,'Authorization':'Bearer '+SUPA_KEY};

// 추가된 함수
storeApiKey(service, key)     // 서버에 암호화 저장
proxyApiCall(service, path, params)  // 서버 경유 API 호출
_deleteApiKeyOnServer(service)       // 서버 키 삭제
resetGoogleAiKey()                   // Google AI 키 초기화 UI
```
- `FKEY` / `CKEY`: 실제 키 대신 `'***'` (flag-based) 저장
- Finnhub 직접 호출 8곳 → `proxyApiCall('finnhub', ...)` 로 변경
- Google AI 직접 호출 → `proxyApiCall('googleai', ...)` 로 변경
- `connectAPI()` → 키 직접 검증 후 `storeApiKey('finnhub', ...)` 호출
- `savePnKey()` → async 변환, `storeApiKey('googleai', ...)` 호출

### 트러블슈팅
| 문제 | 원인 | 해결 |
|------|------|------|
| Edge Function 파일명 오류 | 함수 파일을 `index.ts` 외 이름으로 생성 | 파일명을 `index.ts`로 변경 |
| CORS 파싱 오류 | 긴 문자열 붙여넣기 중 `content-type` 앞에 개행 삽입 | 별도 const로 분리 |
| 401 Unauthorized | Supabase 기본 JWT 검증이 익명 요청 차단 | Publishable Key를 `apikey` + `Authorization` 헤더에 추가 |

---

## 세션 3 — 기반 강화 Phase 2: 디자인 시스템 토큰화

### 배경
- 하드코딩된 색상·폰트 값이 CSS + JS 인라인 스타일 전체에 산재
- 단일 변경 포인트 없이는 테마 변경, 다크모드, 브랜드 조정 불가

### 작업 내용

#### 1. CSS 커스텀 프로퍼티 `:root` 블록 신설
총 **90개 토큰** 정의:
- 색상 토큰 78개 (Text 10 / Surface 8 / Border 6 / Success 9 / Error 5 / Warning 8 / Info 6 / Accent 11 / Trade 3 / Broker 9 / 나머지 3)
- 타이포그래피 10개 (`--text-2xs` ~ `--text-3xl`)
- 간격 8개 (`--space-1` ~ `--space-8`)
- 보더 레디어스 7개 (`--radius-sm` ~ `--radius-full`)

#### 2. 전량 토큰 치환
- 파일 전체에서 **72개 고유 hex 색상값** → `var(--color-xxx)` 참조
- **10가지 font-size 값** → `var(--text-xxx)` 참조
- CSS 클래스, HTML 속성, JS 템플릿 리터럴 (`TAG`, `ACCT_COLOR`, `renderStocks` 등) 모두 포함
- 치환 후 파일 내 잔여 raw hex: **0개** (`:root` 정의 제외)

#### 3. 접근성 (WCAG 2.1)
- `.btn`, `.fbtn`, `.tbtn`, `.nav-tab`, `.itab`, `.memo-vtab` → `min-height: 44px` (터치 타겟 2.5.5)
- `:focus-visible` 포커스 링 (`2px solid var(--color-info)`)
- 아이콘 전용 버튼 `aria-label` 추가 (↻ 새로고침, ✕ 삭제/취소, ✓ 저장)

#### 4. 다크모드 준비
- `@media (prefers-color-scheme: dark)` 블록 추가
- Surface 8 / Border 4 / Text 6개 토큰 오버라이드 정의
- Semantic 색상(success/error/warning/info)은 유지

#### 5. 산출물
- `designsystem_richbuild_v2.md` — 전체 토큰 레퍼런스 문서

---

## 세션 4 — Phase 1: 안정화 (2026-06-19)

### 1-1. XSS 취약점 제거

#### 추가 헬퍼 함수
```js
escapeHtml(s)   // HTML 특수문자 이스케이프 (&, <, >, ")
safeUrl(u)      // javascript: 프로토콜 차단, http/https만 허용
_jsStr(s)       // onclick 속성 내 문자열 이스케이프 (\, ')
```

#### 적용 위치
| 위치 | 대상 | 위험도 |
|------|------|--------|
| `renderMemos()` | `m.text` (메모 본문) | 높음 |
| `renderArchive()` | `m.text` (아카이브 본문) | 높음 |
| `editMemo()` textarea | `m.text` | 높음 |
| `renderStocks()` | `s.name`, `s.ticker`, `acctCh` | 높음 |
| `displayNews()` | `n.headline`, `n.source`, `n.summary`, `n.url` | 중간 |
| `loadCompanyNews()` | `n.headline`, `n.source`, `n.summary`, `n.url` | 중간 |
| 포트폴리오 소식 피드 | `s.ticker`, `s.name`, 뉴스 필드 전체 | 중간 |
| `renderTickerDropdown()` | `r.sym`, `r.name` | 중간 |

#### CSS 추가
```css
.memo-text { white-space: pre-wrap; word-break: break-word; }
/* 메모 줄바꿈 보존 */
```

### 1-2. Supabase 키

이미 JWT anon 키로 교체 완료 상태 확인 → 작업 불필요 (스킵)

### 1-3. 페이지 상태 복원 개선

- 뉴스 서브탭(`cur_itab_news`) 새로고침 후 복원
- 종목별 뉴스 입력값(`news_company_sym`) localStorage 저장 및 복원
- 월가 레이팅 입력값(`news_analyst_sym`) localStorage 저장 및 복원

#### 수정 함수
- `initNewsUI()` — 서브탭 + 입력값 복원 로직 추가
- `loadCompanyNews()` — 검색 시 `news_company_sym` 저장
- `loadAnalystRating()` — 조회 시 `news_analyst_sym` 저장

---

## 세션 5 — Phase 2: UX 개선 (2026-06-19)

### 2-1. 종목 검색 자동완성 키보드 탐색

#### 추가 변수 및 함수
```js
let _tsActiveIdx = -1;               // 현재 선택된 드롭다운 인덱스

tickerSearchKeyDown(e)               // ↑↓ Enter Esc 키보드 이벤트 처리
_tsHighlight(items)                  // 선택 항목 배경 강조
_tsHoverOn(el, idx)                  // 마우스 호버 시 인덱스 동기화
_tsHoverOff(el, idx)                 // 마우스 이탈 시 인덱스 초기화
```

#### 동작
- `↓` : 다음 항목 이동 (마지막에서 멈춤)
- `↑` : 이전 항목 이동 (첫 번째에서 멈춤)
- `Enter` : 현재 선택 항목 클릭 실행
- `Esc` : 드롭다운 닫기
- 마우스 호버와 키보드 선택 상태 동기화
- 드롭다운 아이템에 `data-ac-item` 속성 추가 (쿼리 기준)

### 2-2. 뉴스 → 일지 원클릭 전환

#### 추가 함수
```js
saveToMemo(headline, url)
// 1. 일지 탭으로 이동 (goPage)
// 2. #memo-txt에 "[뉴스] {headline}\n{url}" 삽입
// 3. 100ms 후 포커스
```

#### 버튼 삽입 위치
- `displayNews()` — 시장 뉴스 카드 하단
- `loadCompanyNews()` — 종목별 뉴스 카드 하단
- 포트폴리오 소식 피드 메인 카드 + 더보기 아이템 하단

### 2-3. 모바일 레이아웃 최적화

#### 추가 미디어 쿼리
```css
@media (max-width: 768px) {
  /* 네비 패딩 축소, 탭 폰트 축소, 페이지 패딩 축소 */
  /* 메트릭 그리드: 6열 → 3열 */
}
@media (max-width: 480px) {
  /* 로고 숨김, 이메일 숨김, 탭 최소화 */
  /* 메트릭 그리드: 3열 → 2열 */
  /* 메모 입력 폼: 가로 → 세로 스택 */
  /* 모달 패딩 축소 */
}
```

---

## 세션 6 — Phase 3: AI 기능 (2026-06-19)

### AI 기능 설계 원칙
- Google AI 키(`CKEY`) 미등록 시 AI 섹션 전체 미표시
- 모든 AI 결과 오늘 날짜(`YYYYMMDD`) 기준 localStorage 캐시
- 추가 API 호출 최소화 — 이미 로드된 데이터 재활용

### 3-1. AI 뉴스 브리핑 (기존 기능 고도화)

**변경 대상:** `runAiSummary()`, `translateNewsItems()`

| 항목 | 기존 | 변경 |
|------|------|------|
| 대상 종목 | 티커 있는 전체 | `status === 'hold'` 보유 종목만 |
| 요약 분량 | 최대 3문장 | 2문장 |
| 버튼 텍스트 | AI 요약 | 🤖 AI 뉴스 브리핑 |

### 3-2. 규제 AI 요약 (신규)

**위치:** 리서치·규제 페이지 > 규제 모니터링 탭 > 2컬럼 그리드 위

```js
generateRegSummary()
// 1. CKEY 없으면 안내 메시지
// 2. regAllItems 없으면 갱신 유도
// 3. 상위 3건 → Gemini → 각 2문장 한국어 요약
// 4. JSON 파싱 실패 대비 fallback 처리
// 5. 오늘 날짜 기준 localStorage 캐시 ('ai_reg_YYYYMMDD')
// 6. 재클릭 시 토글 표시/숨김
```

**Gemini 프롬프트:**
```
다음 규제 정보 3개를 각각 한국어 2문장으로 요약해줘.
핵심 내용과 투자자에게 미치는 영향 위주로. JSON 배열만 출력
```

### 3-3. 주간·월간 투자 보고서 (신규)

**위치:** 투자 일지 탭 최상단 고정 카드

#### 설정 UI
- 주간: 요일 선택(월~일) + 시간 선택
- 월간: 일자 선택(1~28일) + 시간 선택
- 저장 키: `ai_report_settings`

#### localStorage 키 구조
```
ai_report_settings   → { weekDay, weekHour, weekMin, monthDay, monthHour, monthMin }
ai_weekly_2026W25    → { text, generatedAt }
ai_monthly_202606    → { text, generatedAt }
rpt_week_dismiss     → "2026W25"  (나중에 클릭 시 해당 주 dismiss)
rpt_month_dismiss    → "202606"
```

#### 신규 함수 목록
```js
_getWeekKey(date)          // "2026W25" 형식 주차 키 생성
_getMonthKey(date)         // "202606" 형식 월 키 생성
toggleReportSettings()     // 설정 패널 열기/닫기
saveReportSettings()       // 설정 localStorage 저장
generateWeeklyReport()     // 7일 내 일지 → Gemini → 3~4문장 요약
generateMonthlyReport()    // 30일 내 일지+아카이브 → Gemini → 4~5문장 요약
_callGeminiReport(prompt)  // Gemini 단순 텍스트 호출 헬퍼
_buildReportHtml(...)      // 보고서 카드 HTML 생성
renderReportSection()      // 저장된 보고서 화면 렌더링
checkReportSchedule()      // 접속 시 발행 시점 도래 여부 체크 → 배너 표시
handleReportBanner()       // "지금 생성" 클릭 처리
dismissReportBanner()      // "나중에" 클릭 처리 + dismiss 저장
_cleanOldReports(prefix,n) // 오래된 보고서 자동 정리
```

#### 스케줄 체크 실행 시점
- 앱 초기화 시 1.5초 후 (`setTimeout(checkReportSchedule, 1500)`)
- 투자 일지 탭 이동 시 (`goPage('memo', ...)`)

#### 보고서 보관 정책
- 주간: 최근 8주 보관 후 초과분 자동 삭제
- 월간: 최근 4개월 보관 후 초과분 자동 삭제

---

## 세션 7 — 계정별 API 키 저장 + AI 모델 수정 + CORS 해결 (2026-06-19)

### 7-1. 계정별 API 키 서버 저장 (Per-user API Key)

#### 배경
- 기존: API 키가 `api_keys` 테이블에 `service TEXT PRIMARY KEY` 단일 키 → 모든 사용자가 공유
- 목표: 계정별 독립 저장, 로그인 시 자동 복원

#### DB 마이그레이션
```sql
-- 기존 테이블 초기화 후 재구성
ALTER TABLE api_keys DROP CONSTRAINT api_keys_pkey;
ALTER TABLE api_keys ADD COLUMN user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE;
ALTER TABLE api_keys ADD PRIMARY KEY (user_id, service);
```

#### Edge Function (`proxy-api/index.ts`) 변경
```ts
// getUserId: Authorization JWT에서 user_id 추출
async function getUserId(req, sb): Promise<string | null>

// 모든 액션(store-key, key-exists, delete-key, call)에 user_id 스코프 적용
// naver-news 액션 추가: 네이버 주식 뉴스 CORS 우회 프록시
// naver-search 액션 추가: 종목 자동완성 프록시
```

#### index.html 변경
```js
// 추가
let _currentSession = null;
function _apiHeaders()  // access_token 있으면 JWT, 없으면 anon 키 반환

// storeApiKey, proxyApiCall, _deleteApiKeyOnServer → _sh 대신 _apiHeaders() 사용

// 추가 함수
async function _restoreApiKeyState()
// 로그인 시 서버에서 finnhub/googleai 키 존재 여부 확인 → UI 자동 복원
// _loadFromCloud() 완료 후 호출
```

#### onAuthStateChange 처리
- `SIGNED_IN`: `_currentSession = session` 설정
- `SIGNED_OUT`: `_currentSession = null` 초기화

### 7-2. AI 모델 수정 (Gemini)

| 단계 | 모델 | 결과 |
|------|------|------|
| 최초 | `gemini-1.5-flash` (v1beta) | Not found 오류 |
| 1차 변경 | `gemini-2.0-flash` | 할당량 0 (limit:0) |
| 최종 | `gemini-1.5-flash-8b` | 무료 티어 정상 작동 |

Edge Function Gemini 엔드포인트:
```
https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash-8b:generateContent?key=${apiKey}
```

### 7-3. 한국 주식 뉴스 CORS 수정

- 기존: `allorigins.win` 공개 프록시 → `m.stock.naver.com` 차단
- 해결: Edge Function에 `naver-news` 액션 추가 → 서버에서 직접 Naver API 호출
- `fetchStockNews()` 한국 종목 분기: `naver-news` 액션으로 변경

### 7-4. Finnhub 포트폴리오 뉴스 타이밍 수정

- 기존: `loadPortfolioNews()` 가 `_currentSession` 설정 전에 실행 → 401
- 해결: `loadPortfolioNews()` 호출을 Auth IIFE 내부로 이동 (세션 확인 이후 실행)

---

## 세션 8 — AI 롤백 + 버그 수정 + 투자 일지 고도화 (2026-06-19)

### 8-1. AI 기능 롤백

세션 6에서 추가한 AI 기능(투자 보고서, 규제 AI 요약)을 수동 제거. 이후 안정화에 집중.

**제거 항목:**
- 투자 일지 상단 주간/월간 보고서 카드 HTML
- 리서치 탭 규제 AI 요약 버튼
- AI 관련 JS 함수 전체: `_getWeekKey`, `_getMonthKey`, `toggleReportSettings`, `saveReportSettings`, `_callGeminiReport`, `_buildReportHtml`, `renderReportSection`, `generateWeeklyReport`, `generateMonthlyReport`, `_cleanOldReports`, `checkReportSchedule`, `_showReportBanner`, `handleReportBanner`, `dismissReportBanner`, `generateRegSummary`
- `goPage()` 내 `checkReportSchedule` 호출
- init 내 `renderReportSection()`, `setTimeout(checkReportSchedule,1500)` 호출
- `translateNewsItems` 프롬프트 원복: 2문장 → 3문장

**유지 항목:** 계정별 API 키 저장, CORS 수정, Finnhub 타이밍 수정, AI 뉴스 요약(포트폴리오 소식)

### 8-2. 버그 수정 (안정화)

| 심각도 | 문제 | 원인 | 해결 |
|--------|------|------|------|
| 🔴 Critical | 앱 로드 시 뉴스탭 "API 키 오류" 잘못 표시 | 페이지 복원이 Auth IIFE 완료 전 실행 → `_currentSession=null` 상태로 Finnhub 호출 → 서버 401 | 페이지 복원 코드를 Auth IIFE 내부 마지막으로 이동 |
| 🟠 High | 로그인 후 뉴스탭 자동 갱신 안 됨 | `_restoreApiKeyState()`가 FKEY 복원 후 뉴스탭 상태 미확인 | FKEY 복원 시 뉴스탭 활성이면 `loadMarketNews()` + `refreshQuotes()` 재호출 |
| 🟡 Medium | 비회원 API 키 연결 시 "서버 저장 실패" 혼란 | 에러 메시지가 원인 미설명 | `_currentSession=null` 조건 추가 → "로그인 후 API 키를 저장할 수 있습니다" 안내 |
| 🟢 Low | auth IIFE 내 `const _sh` 섀도잉 | 전역 `_sh`(헤더 객체)와 같은 이름 | `_summaryHtml`로 이름 변경 |

### 8-3. 모달 외부 클릭 닫기

```html
<!-- 로그인/회원가입 -->
<div id="auth-overlay" onclick="if(event.target===this)closeAuthModal()">

<!-- 프로필 설정 -->
<div id="profile-overlay" onclick="if(event.target===this)closeProfileModal()">

<!-- 온보딩 -->
<div id="ob-overlay" onclick="if(event.target===this)document.getElementById('ob-overlay').classList.add('hidden')">
```

`event.target === this` 조건: 배경 클릭만 닫힘, 카드 내부 클릭은 무시

### 8-4. 투자 일지 레이아웃 개선

**변경 전:** 탭 → 일지 목록(`max-height:420px` 고정) → 작성폼
**변경 후:** 탭 → 작성폼 → 일지 목록(높이 제한 없음, 페이지 스크롤)

- 작성폼(`#memo-compose-area`) 상단으로 이동 → 진입 즉시 작성 가능
- `memo-list` `max-height` 제거 → 일지 전체 페이지 스크롤로 표시
- 구분선: `border-top` → `border-bottom` (폼 아래 구분)

### 8-5. 아카이브 태그 필터

아카이브 탭 진입 시 태그 필터 버튼 표시.

```js
let archiveTagFilter = 'all';

setArchiveFilter(tag, btn)   // 필터 선택 + UI 업데이트 + 재렌더
// renderArchive() 내부에서 archiveTagFilter 적용
```

- 필터 버튼: 전체 / 일반 / 매수 / 매도 / 매크로 / 복기 / 아이디어
- 탭 전환 시 필터 자동 초기화 (`archiveTagFilter = 'all'`)
- 해당 태그 항목 없으면 "해당 태그의 아카이브가 없습니다." 표시

### 8-6. 투자 철학 탭 신설

#### 개요
아카이브/활성 일지 메모를 개인 투자 원칙으로 분류해 관리하는 컬렉션.

#### 탭 구조
```
[활성 일지]  [아카이브]  [투자 철학]  ← 신규
```

#### UI
```
┌──────────────────────┬──────────────────────┐
│  ✅ 지켜야 할 것      │  ❌ 절대 하면 안 되는 것 │
│  (must)              │  (never)             │
└──────────────────────┴──────────────────────┘
```
- 모바일: `@media(max-width:480px)` → 1컬럼으로 전환
- 각 항목: `↑ ↓` 순서 조정, 삭제 가능

#### 등록 경로
1. 아카이브 카드 → `[✅ 지켜야 할 것]` / `[❌ 절대 금지]` 버튼
2. 활성 일지 카드 → 동일 버튼 (아카이브 거치지 않고 바로 등록)
- 이미 등록된 항목은 `✓ 지켜야 할 것` / `✓ 절대 금지` 로 표시

#### 신규 함수
```js
_philoBtns(sourceId)           // 등록 상태에 따라 버튼 HTML 반환
addToPhilosophy(sourceId, type)// memos + memoArchive 둘 다 탐색해 등록
removePhilosophy(id)           // 철학 컬렉션에서 삭제 (원본 유지)
movePhilosophy(id, dir)        // 같은 type 내 순서 이동 (배열 인덱스 swap)
renderPhilosophy(el)           // 2컬럼 렌더링
```

#### 데이터 구조
```js
investPhilosophy = [
  { id, text, type: 'must'|'never', sourceId, createdAt }
]
```

#### 저장
- localStorage: `pf_phil`
- 클라우드: Supabase `user_data.invest_philosophy` (JSONB)

#### DB 마이그레이션
```sql
ALTER TABLE user_data ADD COLUMN IF NOT EXISTS invest_philosophy JSONB DEFAULT '[]'::jsonb;
```

---

## 세션 9 — 교착 해소·회원가입 UX·시세/뉴스 유지보수·캘린더 어닝콜 (2026-06-30~07-01)

> 커밋: `fb0ac92`, `a77e648`, `62ed39c`, `7672521` (모두 main 배포)
> 검증: Chrome 자동화로 라이브(github.io) + localhost 로그인 세션에서 end-to-end 테스트

### 9-1. supabase-js init 교착 근본 해소 (★ 기업모니터링 "등록" 먹통 원인)
- **증상**: 기업 모니터링 DART 키 "등록" 클릭이 먹통, 키 상태 미복원으로 항상 샘플 표시, 클라우드 동기화 중단.
- **근본 원인**: GoTrue `_initialize`가 lock 안에서 `onAuthStateChange` 콜백을 await → 콜백의 `_loadFromCloud`가 `_sb.from()`(타임아웃 없음)을 호출 → `initializePromise` 순환 대기로 **lock 영구 점유**(`lockAcquired:true`, `pendingInLock:1`) → 이후 `getSession`/`getUser`/`_sb.from()` 전부 무한 정지. lock no-op으로도 못 고침(초기화 Promise 자체가 미완).
- **해결**: 교착 취약 DB 입출력을 REST(PostgREST)로 전환. 헬퍼 `_restSelectRow`/`_restUpsert`/`_safeGetSession` 추가, `_loadFromCloud`·`_syncToCloud`·`obSubmit`·`saveProfile`·`openProfileModal`·`_restoreApiKeyState`·`connectAPI`·`saveDartKey`·init IIFE 적용 + 중복 로드 가드(`_cloudLoadedFor`).
- **검증**: 라이브에서 `getSession 0ms`, lock 미점유, 등록 1.5초 완주, 키 복원("관리자 키") 정상.

### 9-2. 회원가입 UI/UX 개선
- 비밀번호 표시/숨김 토글, 실시간 비밀번호 강도 막대(약함→안전), 이메일 형식·비밀번호 일치 즉시 안내, 안내 문구, "이미 계정이 있으신가요? 로그인" 링크. 기존 ID/`doSignUp` 유지.

### 9-3. 온보딩 기업 계정 맞춤 질문
- 기업 선택 시 step3를 기업 모니터링 목적(경쟁사 공시/지분변동/DART/규제/시장동향/공시알림)으로 분기 + 맞춤 타이틀. 개인↔기업 전환 시 목적 초기화.

### 9-4. 기업 모니터링 메모 → 투자 일지 캘린더 자동 저장
- `saveMonitorMemo` 시 `memoArchive`(캘린더)에 `[회사명] 메모`로 자동 기록(`journalId` 연계), 메모 행에 "📅 일지 저장됨" 배지. (일지는 영구 기록 → 모니터 메모 삭제 시 미러 삭제 안 함)

### 9-5. 국내 시세 정확도(KOSDAQ) 수정
- `dart-stock-price`가 모든 종목에 `.KS` 고정 → KOSDAQ 시세 오답(에코프로비엠 .KS=194,000 vs 정답 142,500).
- **해결**: `_fetchCompanyPrice(code, market)` — Naver(`m.stock.naver.com`, 6자리 코드만으로 정확) 우선 + Yahoo(시장구분 `.KS`/`.KQ`) 폴백. KOSPI 값 동일 유지.
- ※ 삼성전자 334,000·SK하이닉스 2,650,000은 Naver와 동일한 **정상 현재값**이었음(버그 아님).

### 9-6. 뉴스 페이지 점검 + 월가 레이팅 미표시 해소
- 4탭(시장뉴스/종목별/월가레이팅/실시간시세) 라이브 점검. 월가 레이팅이 기업/관리자 계정에서 "데이터 없음".
- **원인**: 클라이언트가 biz 사용자를 레거시 `finnhub-proxy`(경로 제한)로 보내 `stock/recommendation`이 `path_not_allowed`. → 전체 경로 지원·승인기업 관리자키 적용하는 `fh-call`로 일원화. (`stock/price-target`은 Finnhub 유료 403 → 컨센서스만 표시)

### 9-7. 투자 일지 캘린더 어닝콜 표기 (US + KR)
- **US (Stage 1)**: 포트폴리오 US 종목의 Finnhub `calendar/earnings`(예정일)을 `proxy-api 'call'`로 조회(fh-call 화이트리스트 우회). 🎙️ 표기, 장 시작전/마감후 표시.
- **KR (Stage 2)**: DART 잠정실적(영업잠정실적 공정공시=실제 어닝) + 정기보고서 공시일 + 다음 분기 예상일(최근 잠정실적+91일). corp_map→corp_code→list.json.
- 캘린더 진입 시 US/KR 동시 로드, localStorage 캐시(US 6h, KR 24h). EV_STYLE에 `stockearn`(🎙️) 추가.
- **검증**: 라이브 7월 캘린더에 IBM/INTC/VRT/AMZN 어닝콜 + 삼성전자 어닝콜(예상 7/29)이 FOMC·CPI와 함께 표시.

### 미해결/후속 후보
- 실시간 시세에 사용자가 추가한 잘못된 티커(`ANT`)는 "——" 표시(코드 버그 아님, 데이터 정리 필요).
- 프로필 설정 모달의 사용 목적 체크박스는 개인용 5개만 → 기업 사용자용 항목 미반영.
- Edge Function(`market-data`)은 별도 배포라 `.KS` 고정/`finnhub-proxy` 화이트리스트는 코드 우회로 처리(소스 수정은 재배포 필요).

---

## 주요 파일 현황

| 파일 | 역할 |
|------|------|
| `index.html` | 전체 앱 (HTML + CSS + JS), 현재 약 5,300줄 |
| `designsystem_richbuild_v2.md` | 디자인 토큰 레퍼런스 |
| `supabase/functions/proxy-api/index.ts` | Edge Function 소스 (참조용) |
| `개발내역.md` | 본 문서 |

## 환경 정보

| 항목 | 값 |
|------|-----|
| Supabase 프로젝트 | `yijkwuiqnviapztqskak` |
| Edge Function URL | `https://yijkwuiqnviapztqskak.supabase.co/functions/v1/proxy-api` |
| Supabase JWT anon 키 | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` (코드 line 800) |
| GitHub Pages | `https://jinhoo111.github.io/-_-/` |
