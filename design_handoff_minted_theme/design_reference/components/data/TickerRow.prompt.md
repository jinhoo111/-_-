List row for holdings, watchlists, movers.

```jsx
<TickerRow symbol="SE" name="Samsung Elec" sub="12 shares" value="₩8,940,000" change={<PriceChange value={2.1} size="sm" />} onClick={open} />
```
Stack rows directly; no dividers needed (rows are ≥56px with hover fill).
